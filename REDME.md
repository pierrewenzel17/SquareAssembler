<h1>Square Assembler</h1>

<h3>Installation</h3>
Installation des dépendances 
```shell
pip install -r requirements.txt
```
Remise à niveau des dépendances depuis la VM
```shell
 pip freeze > requirements.txt
```